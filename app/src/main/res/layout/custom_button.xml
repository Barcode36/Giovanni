<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:foreground="?android:attr/selectableItemBackground"
    card_view:cardCornerRadius="@dimen/dimen_15dp"
    card_view:cardElevation="5dp"
    card_view:cardUseCompatPadding="true"
    style="@style/CustomButton">

    <!-- android:foreground="?android:attr/selectableItemBackground" serve per l'effetto animato quando si preme il bottone -->

    <RelativeLayout
        android:id="@+id/relative"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true">

        <View
            android:id="@+id/background"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/button_selector">
        </View>

        <ImageView
            android:id="@+id/image_android"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:adjustViewBounds="true"
            android:scaleType="centerInside"
            card_view:srcCompat="@android:drawable/sym_def_app_icon" />

        <TextView
            android:id="@+id/text_android"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/image_android"
            android:layout_centerHorizontal="true"
            android:gravity="center_horizontal"
            android:text="@string/android"
            android:textColor="@color/green"
            android:textSize="18sp" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>